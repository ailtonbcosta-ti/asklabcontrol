# Banco de dados (opcionais — padrão: asklab)
POSTGRES_USER=asklab
POSTGRES_PASSWORD=asklab
POSTGRES_DB=asklab

# JWT — OBRIGATÓRIOS — gere com: openssl rand -base64 32
JWT_ACCESS_SECRET=troque-por-string-aleatoria-minimo-32-caracteres
JWT_REFRESH_SECRET=troque-por-outra-string-aleatoria-minimo-32-caracteres

# Portas do host (opcionais — padrões abaixo)
BACKEND_PORT=3001
FRONTEND_PORT=8080
